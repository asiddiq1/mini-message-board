<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
</head>
<body>
  <h1><%= title %></h1>
  <ul>
    <% messages.forEach((message, index) => { %>
    <li><%= message.text %></li>
    <button onclick="location.href='/<%= message.user%>/<%= index %>'">Open message</button>
  <% }); %>
  </ul>
<h5><a href="/new">Add new message</a></h5>
</body>
</html>